<UserControl x:Class="ACViewer.View.CustomPalettePreviewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinWidth="260" MinHeight="220">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel Grid.Row="0" Margin="4,4,4,2">
            <TextBlock Text="Palette Preview" FontWeight="Bold"/>
        </DockPanel>
        <ListBox x:Name="PaletteList"
                 Grid.Row="1"
                 Margin="4"
                 HorizontalContentAlignment="Stretch"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 SelectionChanged="PaletteList_SelectionChanged"
                 MouseDoubleClick="PaletteList_MouseDoubleClick"/>
        <Border Grid.Row="2" Margin="4" Padding="4" BorderBrush="#555" BorderThickness="1">
            <StackPanel Orientation="Vertical">
                <TextBlock Text="Selected Palette" FontWeight="Bold"/>
                <Image x:Name="SelectedPreview" Height="40" Stretch="Fill" Margin="0,4,0,4" SnapsToDevicePixels="True"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="BtnApply" Content="Apply" Width="80" Margin="0,0,6,0" Click="BtnApply_Click"/>
                    <Button x:Name="BtnReload" Content="Reload" Width="70" Click="BtnReload_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
